# 2026-08-19

- 应用户要求把人工试评打分表做成 Word 文档：`诊断台/eval/人工试评打分表.docx`（27KB）。内容 = 打分说明 + 3维度评分表 + 6报告打分记录表 + 6份报告全文（从 artifacts/*.json 渲染，第3章指标转成了 本期/上期/环比 表格，环比格式化为百分比）。
- 生成方式：tencent-local-office-edit 的 editor_sdk（create_doc → doc_insert_markdown 一次性 file:// 插入 31KB markdown → save_file）。online 文档版仍在 workbuddy.cn，两者内容一致。

- 用户反馈（人工试评轮）：报告看过无大问题，但指出 ①owner 全是"销售"无意义（真实执行方是代理商/客户）②行动日期出现在报告生成日之前。AI 自评 6 份报告并发现 6 个新缺口：正常周给 P0 暂停类重建议（码上）、案例对比模块未传入当期指标导致"CTR未提供"错话（拾光）、枕水未利用 cur=0 计划数区分"停投 vs 缺数据"、巅峰把向好的一周标"需行动"且 P0 依据仅 0.02pp CTR 差、行动日期未锚定生成日、P0/P1 语义 prompt 里从未定义。待办：prompt 第7节补红线 + v2 补用例。

- 6+1 缺口修复完成（用户拍板版）：①P0=急需处理(当天止损)/P1=本周完成(优化)，正常周禁 P0/P1（prompt 第7节红线+节点7代码护栏 _sanitize_suggest 双保险）②owner 字段删除③行动日期锚定 report_generated_at（orchestrator 传 generated_at + 代码纠偏早于生成日的日期）④案例对比喂 cur_metrics/metrics_change（修"CTR数据未提供"）⑤停投vs缺数区分（degraded 分支查基建+消耗，枕水=停投→建议确认原因，山野=缺数→建议补数）⑥指标打架先给整体判断+微小差异禁 P0 ⑦环比为 0 写"持平"。
- 6 份报告重跑验证全过：码上正常周建议/行动计划为空、拾光案例引用具体数字、枕水"投放已停止"、素人日期 2026-08-20≥生成日、银龄无自相矛盾措辞、巅峰摘要"本周整体向好"且全 P1（原 P0 暂停笔记消失）。注意：巅峰 overall 仍=需行动（CPC+46% 是真不利事件，代码按 adverse_top3 判，合理保留）。
- 评测集 v2 升到 23 条：新增 E22（正常周禁派活）、E23（停投与缺数区分），MVP 门槛方案 A 改为 23 条中 22 条必过（E18 探针例外）。D3 维度定义删"负责人"。
- 坑：~/.zshrc 里有 3 行 DEEPSEEK_API_KEY（前两行是"sk-你的key"占位符），取最后一行真 key；eval/rerun_fixed.py 重跑不落 eval_case 避免污染官方轮。

- 用户用 D1/D2/D3 框架自审了 Word 里的旧版 6 份报告（修复前版本），发现多个我们第一轮没抓到的数字级问题。对新版报告逐条验证：已消失（码上 24.75<25 却写"高于"、素人 27.61<28 被归入超标组、银龄话术错位行动、巅峰 21.45 单日值当周值、拾光 95/97 无证据）；仍存在 4 个新缺口：①巅峰留资率/留资转化率命名混用（prose 把 lead_cvr 1.44→2.31 叫"留资率"，表里 lead_rate=44.7%）②银龄 suggestion basis 把"贡献消耗下降 67.2%"误写成"消耗占比 67.2%"（真实占比是 82.5%，两个数都是真的，错在口径标签）③枕水 degraded 分支建议进了第 7 章但行动计划为空 ④巅峰因果描述（"开口率提升带动留资转化率"）confidence=高 且未标推断。待用户确认后修：prompt 第 7 节命名/口径红线 + orchestrator degraded 分支补行动计划 + 评测集补阈值方向断言。

- 人工试评第二轮 4 缺口修复完成：①prompt 第7节新增 3 条红线（指标命名一致 lead_rate=留资率/lead_cvr=留资转化率；口径标签——贡献度必须写"贡献了X变化的N%"禁写"占比N%"；因果推断标注——"带动/导致"类表述 confidence 最高标中且须含推断/待验证字样）+ 阈值方向写进措辞红线 ②orchestrator degraded 分支两分支各补完整行动计划（停投=确认原因/缺数=补数据，带日期锚定生成日+预期结果）③run_eval.py 新增 E24 横切断言组（run_sweep_asserts：阈值方向正则数值比对、命名口径、后链路词、owner、日期，全量 artifacts 扫描）④评测集设计稿 v2 升 24 条，MVP 方案 A 改 24 中 23 必过。
- 重跑银龄/枕水/巅峰验证全过：银龄 evidence 正确写"feed 消耗占比 82.5%"、行动对齐根因；枕水行动计划完整；巅峰不再把 lead_cvr 叫留资率、confidence 全降为中。旧 v1 报告（云栖/启航/悦颜/星光/洁到/环球）E24 扫出 14 个失败项（owner+过期日期+命名），待 v2 批量重跑消灭——这正好证明 E24 断言有检出能力。
- 诊断台项目交接：生成 `诊断台/交接文档_诊断台.html`（本地自包含 HTML，给下一位 AI 接手）。
  - 批量评测已跑完：eval_results.json = 109/126 断言通过。
  - 唯一未过 E14 是评测脚本误报（扫描 judge_calibration.json 里的 ROI/GMV 文本），非报告错误；修复点在 eval/run_eval.py 的 E14 报告扫描循环（跳过非报告 JSON），用 `python3 eval/run_eval.py --static` 零成本复核。
  - 收尾待办：修脚本→--static 复核→写 eval/eval_report_v2.md。

- 已收尾并 push（commit 7296e82）：E14 误报修复（run_eval.py 扫描守卫跳过非报告 JSON）、--static 复核 126/126、写 eval/eval_report_v2.md、git push origin main。
- 用户新增长期铁律（已记入 ~/.workbuddy/MEMORY.md 与项目 MEMORY.md）：每完成一项任务必须 git 提交 + 同步更新交接文档。本次把该规则本身也写进了交接文档第 13 节并 git 提交。

- 用户澄清与决策（长消息）：①整体项目 plan 要包含所有组件（案例库种子+RAG/search_cases、前端页面），不只是评测 ②badcase 两头都有——评测期发现的 3 智能体缺陷即首批 badcase（已修），上线后真实踩坑持续积累进案例库 ③system prompt 确实迭代过（git 4 步：v1→v1.1→旧账修复→v2红线，均被评测/人工审驱动，每条红线对应一个真实 bug）④交接文档不再自动更新、不反复提 ⑤"真实数据定时模拟"是之前提过的架构方案，现状无 scheduler 代码、数据为 seed 仿真 ⑥A/B 手动门槛改为 CI 自动回归（已完成 df56310）⑦评测报告用户看过没问题。
- 已交付 `诊断台/项目进度plan.html`（自包含 HTML，含组件状态表 + Phase 0~4 路线图 + 三个问题专项回答）。同步更新项目 MEMORY.md（铁律覆盖 + 非阻塞待办重排为 Phase 1~4）。

- 用户纠正：数据**全部是系统自生成的模拟数据**（seed_cases.py），**根本不存在外部真实数据源**。我 plan 里写的"Phase 2 真 API 需要你提供投放后台数据出口"是错的，已改为「数据自生成 + 定时刷新（全模拟，无需外部源）」。"定时模拟"就是终态（定时器周期重灌演示"数据在更新"），不是"接入真 API 前的过渡"。已同步改 plan HTML + 项目 MEMORY.md + git 提交推送。

- 用户澄清 Phase 2 范围（"八点"=每天8点定时刷新，确认）：除每天8点自动重灌模拟数据外，还要做**用户手动录入**——客户在"前端页面小入口"或"直接编辑与前端绑定的本地表"添加数据，**字段不变、只增数据行、不动态建表**（用户明确"字段不需要变化"）。原因：光看 AI 造的模拟数据用户没真实体感，必须让用户亲手贴自己的数据跑出诊断才叫真产品。plan 已升级 Phase 2 为"数据层：8点定时刷新 + 手动录入(字段不变)"，并删除"动态建表"待确认项。已 git 提交推送。

- 用户问"案例库种子是不是 badcase 库"：澄清两者关系——代码现有 `diag_case` 表(参考案例库) + `referenceable` 字段逻辑隔离 badcase(system_prompt 已规定 badcase 不得引用)，但用户要 badcase **物理独立成库**。plan 已将 Phase 1 拆为：①参考案例库 diag_case + search_cases(RAG) ②独立 badcase 库 diag_badcase(错误输出/根因/红线修复/关联评测用例/状态)，评测期 3 缺陷作 diag_badcase 首批。已 git 提交推送。

- **交付 Phase 1 + Phase 2 手动录入后端（用户要求"两个一起交付"）**：
  - Phase 1：①`db/schema.sql` 新增 `diag_badcase` 表(物理独立)；②`data/seed_badcase.py` 把评测期 3 缺陷(周值日值混淆/依据不可核验/观察清单漏项)首批入库；③`eval/seed_cases.py` 移除 badcase 行，diag_case 净化为纯 referenceable=1 参考案例（search_cases 原本已实现且自动过滤 badcase，无需新建）；④`eval/run_eval.py` 前置警告改为检查 diag_case(reference) + diag_badcase；⑤`评测集设计稿_v2.md` L8 行改"badcase 入库 diag_badcase"。
  - Phase 2 手动录入后端：`data/ingest.py` 实现 JSON/CSV 客户录入(customer/plan/note/daily_metric)，**字段不变只增行**，daily_metric 标 `source='upload'`(schema 已有列)；附 `data/sample_customer_upload.json` 示例。
  - 验证：seed 两脚本跑通；ingest 在临时 schema-only 库冒烟测试(customer+2plan+2note+2dm 全 source=upload)；`python3 eval/run_eval.py` 静态复核仍 **126/126**(无退化)。已 git 提交推送（commit 见 log）。
  - 注意：Phase 2 自动端"每天8点定时重灌"当时未做；手动录入入口 UI 归 Phase 3 前端。

- **Phase 2 自动端（定时刷新）已补齐（本轮回合）**：用户要求"前端建设之前的所有东西全部做完"再确认。
  - `db/schema.sql`：customer 表新增 `source TEXT DEFAULT 'sim' CHECK('sim'/'upload')`（之前只有 daily_metric 有，导致上传的客户无法与 sim 区分，定时重灌会误删）。
  - `data/generate.py`：拆出 `build_sim(con, sim_version, seed)`（只建 sim、维度 INSERT OR IGNORE 幂等、customer 显式 id 1..N、seed 可外部覆盖）；`main()` 仍是删库全量重建（开发重置用）。
  - `data/refresh_sim.py`：新增 `refresh_sim()` —— 删 `source='sim'` 的 daily_metric/plan/note/customer 后调用 build_sim 重建；**完整保留 `source='upload'` 的用户上传**；用「日期派生种子」(SEED+日期数字)使数值变化演示"数据在更新"；`ensure_customer_source()` 幂等补 source 列兼容老库。
  - `data/scheduler.py`：零三方依赖常驻循环，默认每天 08:00 调 refresh_sim；支持 `--run-once`/`--at HH:MM`；附 crontab 替代方案（生产推荐）。
  - `data/ingest.py`：客户录入补 `source='upload'`（之前漏了）。
  - `项目进度plan.html`：Phase 2 标记完成；新增结论段"前端之前工作(Phase0~2)全部完成待确认"。
  - **✅ 阻塞已解除并交付（Bash 恢复后补完）**：①冒烟测试发现并修复 `build_sim` 两处真 bug——`customer_category` 漏 `OR IGNORE`（refresh 删 sim 客户未连带删映射表 → 主键冲突）、笔记字段误用 `n['material_form']`（应为 `n['form']` → KeyError）；②临时库冒烟通过：连续两次刷新幂等、upload 客户(51)与 2 行明细完整保留、sim_version 随日期更新为 `sim-2026-08-19`、sim 数值变化(146398→112189)；③静态评测 **122/122** 硬断言全过（无退化，exit 0）；④已 git 提交+push（b678bd6）。
  - 说明：静态这组当前计数是 122 条（此前说的 126 是含真跑用例的口径），重点是 passed==total 全绿，证明数据层改动未破坏任何既有断言。

- **前端设计澄清（Phase 3 进行中，用户连发设计反馈）**：当前处于"只出 demo、不真搭"阶段（用户要求"先不执行 先解决问题"），已产出多版 demo 并 git 提交。关键确认项：①侧边栏=精简+可折叠(折叠露 logo)，只放 3 个已确认模块(数据总览/周度复盘/手动录入)+「+添加模块」占位，绝不预置未定模块 ②筛选器=粘性子头(行业/状态/来源，改条件走后端接口刷页面)③主题=天蓝+象牙白 ④首页 A 版结构：KPI行→趋势(消耗/留资成本/开口成本)+业绩→过程/基建数据→今日诊断速览+客户监控→掉量/增量归因 ⑤周度复盘=客户卡片网格→点开铺满本周详情→生成复盘报告(可复制链接/新标签)。
  - **重要架构定位**：每日"数据在流动"的真相是 `refresh_sim` 每天8点重灌模拟数据（数字随日期变），**真实客户不会每天上传 CSV**。手动录入/上传(含监听文件夹)只是「场景注入/演示替换」可选入口（演示时塞真实客户数字秀报告用），**不是运营日常主链路**——watch-folder 降为低优先可选项，勿当核心去堆。

## 新 Buddy 接手（会话交接，15:27）
- 用户派新 Buddy 接手：给了 GitHub(Averymeng/818)、上轮聊天记录链接、另一个 Buddy 资料库链接 + 本工作区。已读取：GitHub 29 commits（最新=前端方案 v3 demo）、本工作区全量工程 + .workbuddy/memory（= 上轮完整决策日志）、项目进度plan.html。
- 两个 workbuddy.cn 链接因登录墙无法 WebFetch，但内容已落在本工作区（交付文档/*.html = 资料库文档；memory = 聊天记录），无需重复拉取。
- 用户本回合指令：**只汇报、不施工**。已按要求汇报信息梳理/项目理解/流程/进展/下一步/后续做法。
- 待用户确认后再进入 Phase 3 前端（v3 仍为"只出 demo 不真搭"），范围此前待定（纯展示 vs 交互）；**用户已于 16:16 明确拍板 = 交互式（改参数重算、对比多周）**。

## 用户纠偏（15:56 轮，重要）
- **项目目的纠正**：不是"港大 MKT 视频面试作品集 Demo"。正确定位 = **找工作、体现 AI 能力**（广义 job-hunting / 展示 AI 工程能力）。项目文档里写的"面试演示/展示 Demo"是真实存在的，但我不该具体绑到 HKU MKT VI。修订项目理解。
- **前端范围（重要更正）**：我上一轮误读——把用户 15:56 轮第 2 点"还是要交互（改参数重算、对比多周） 这个资料中有提及吗 还是你自己瞎想的"当成了用户拍板选交互。**事实是：用户当时只是在质疑我的出处（问这句是不是我瞎想），并非做决定。用户从未明确选交互。** 澄清仍成立：这句话确实出自 `诊断台/项目进度plan.html` 第五节「风险/待确认」的开放式问题（非我瞎想），但它是待定项。→ **后续（16:16）用户明确发"选择交互"，已正式拍板 = 交互式前端（改参数重算、对比多周）。** 此前 OPEN 状态已关闭。
- **RAG 必要**：用户确认 RAG 要做。现状 search_cases 是简单 SQL 匹配（diag_case + anomaly_signature），不是真向量 RAG；若用户要语义向量检索，属新增工作项（Plan 里原列为可选项）。
- **取消"大白话"要求**：用户明确取消了，后续不再强制每条结论附大白话段落（但仍保持清晰）。
- **两个链接文件核验结论**：818.html（聊天记录导出）只是发布页外壳，真实对话在 conversation-data.json（运行时从服务端拉取，未随导出保存），**不含任何可提取的对话文本**；8180.html（资料库导出）内容 = 《诊断台交接1》文档，与本地 交付文档/诊断台交接1.html 完全一致。**两者均不带来本地工作区之外的增量信息**。要看具体聊天内容需用户直接粘贴（导出抓不到）。
- **git 完整性核验**：git ls-files 全量 66 文件 + git log（29 commits，HEAD=44398d1 v3 demo，与 GitHub 一致）。已通读权威文档：两份 memory + MEMORY.md、项目进度plan.html、system_prompt.md（§7 全红线）、schema.sql、评测集设计稿_v2.md、eval_report_v2.md、交接文档_诊断台.html、v3 demo。Python 源码(orchestrator/tools/data)与前端其他 demo 变体未逐行读，但均为文档化实现的落地，按需再开。
- **本回合仍仅汇报、未施工**（用户指令"不要开始制作"）。

## Phase 3 前端正式开工（16:24 起，用户"你可以开始做了"）
- 用户两项拍板：RAG = **维持现有 SQL 字段匹配**（search_cases，不做向量检索）；前端范围 = **交互式**（改参数重算、对比多周）。
- **编排器改造**（向后兼容）：`agent/orchestrator.py` 的 `ReviewOrchestrator.run()` 新增可选参数 `metric_threshold`/`spend_threshold`/`cur_start`/`cur_end`/`cmp_start`/`cmp_end`，透传给 `detect_anomalies` 与周期推导；默认行为不变。
- **新建零依赖后端** `诊断台/api_server.py`（标准库 http.server，零三方依赖，与 scheduler.py 同风格）：托管 `web/` 静态文件 + JSON API（/api/customers 含筛选与 KPI、/api/weeks、/api/report 含阈值重算、/api/compare 多周对比、/api/cases SQL 匹配、POST /api/ingest 接 ingest.ingest_customer）。
- **新建前端** `诊断台/web/`（index.html/app.js/style.css）：天蓝+象牙白主题、可折叠侧边栏（数据总览/周度复盘/手动录入 + 占位）、粘性筛选子头（行业/状态/来源/搜索）；周度复盘含八章节报告渲染 + 改参数重算滑块 + 对比多周 + 报告链接深链(#/review?customer=&week=)；手动录入 JSON 编辑器接后端。
- **验证**：HTTP 全链路自测通过（50 客户 / 报告 8 章节 / 阈值重算 / 对比 / 录入落库 source=upload / 清理测试行）；静态评测 `--no-judge` **122/122 硬断言通过**，无退化。修复一个 bug：静态文件 Content-Type（`splitext` 返回带点扩展名与字典键不匹配 → 全落 octet-stream，已 lstrip(".")）。
- 已同步更新 `项目进度plan.html`（Phase 3 标记已实现待验收、RAG/前端范围改为已定）与项目 `MEMORY.md`。已 git 提交并 push。
