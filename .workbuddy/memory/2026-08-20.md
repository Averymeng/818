- **问题诊断：今日消耗全 0 + 归因计算中**（无代码改动）：用户截图显示 8-20 客户每日监控全部 ¥0/-100%，且增量 TOP 卡“归因计算中…”。根因是真实时间已到 8-20，但 SQLite 模拟数据最新只到 8-19（refresh_sim 未跑今天）。因此今日窗口无数据，所有客户环比 −100%；前端 loadAttrib 请求后端 /api/attrib 实际可返回文案，但因页面可能未重载或当天全 0 显得无变化。解决：执行 `python3 data/refresh_sim.py --run-once` 重新生成到 8-20，DB 最新日期变为 2026-08-20。验证 /api/attrib 返回真实归因（如 cid1 “消耗环比 -4%：留资成本 ¥53→¥42”）。建议用户刷新浏览器。
- **销售业绩模块删除 + 趋势模块改造demo**（commit `0b5d713`）：用户确认删除“销售业绩”卡片。app.js 去掉 MONTH_TARGET/sales/pctDone/remain 及整段卡片渲染（保留顶部 KPI 的 `sales=cur.spend`），style.css 将 `.ov-row2` 从 2fr 1fr 改为 1fr（趋势卡独占整行），删除 `.ov-progress` 系列样式。另新增 `demo/趋势模块改造demo_分面小图.html`：用 8-07~8-20 真实 DB 快照（每日 spend/lead/open 汇总）做 3 张独立小图，各自带真实 Y 轴、均值虚线、末端点标注，替代现在“3 条不同量纲线各自归一化叠加”的不可读画法。
- **趋势模块平滑曲线上线**（commit `0de8e79`）：把首页趋势卡改成 3 张独立小图（消耗/留资成本/开口成本），各自带真实 Y 轴刻度、均值虚线、末端点标注；线条统一用 Catmull-Rom 转贝塞尔的 `smoothPath()` 画成平滑曲线，并加了极淡的面积填充。样式新增 `.ov-trend-grid/.ov-trend-mini/.ov-t-axis/.ov-trend-empty`。smoke 测试通过。
- **修复归因占位“归因计算中…”卡住**（commit `ac2f6ec`）：用户截图增量 TOP 全部卡占位。排查确认：后端 POST /api/attrib 毫秒级返回且文案正常（如星光KTV cid44 返回“消耗环比 +10%：留资成本 ¥159→¥128”），非 LLM/API 问题。根因是前端 `loadAttrib` catch 块静默吞掉错误，一旦 fetch/解析抛异常就永远停在“归因计算中…”。修复：1）`attribHTML` 初始渲染改为真实指标摘要兜底（原模板文案）；2）API 成功后再把兜底替换成后端归因；3）catch 块 `console.error` 并在异常时把占位恢复为兜底。smoke_web.js 归因断言同步加强。刷新页面生效。
- **趋势图改回折线 + X轴排版优化**（commit `6753529`）：用户要求分面小图线条换回折线，且X轴标签避免重叠。app.js：删除 smoothPath() 平滑曲线，改为 linePath() 直线连接；miniTrend 中 W 240→260、H 132→150、PB 18→28，step 由 round 改为 ceil 保证最多5个刻度，X轴标签添加 rotate(-35) 和 text-anchor="end" 斜放；svg 内每个数据点画小圆点（末端高亮），去掉面积填充。style.css 同步 svg height 132→150。smoke 测试通过。
- **KPI卡片太空：输出3个设计demo**（commit `6753529`）：未直接修改正式页面，新增 demo/KPI改造demo_A_紧凑图标式.html、B_左大右小带迷你图.html、C_信息填充式.html，分别展示紧凑图标、左大右小+sparkline、信息填充+进度条三种方向，供用户选择。
- **KPI卡片选方案A落地**（commit `c3205dd`）：用户在3个demo中选A（紧凑图标式）。app.js 三个KPI卡改为 `.ov-kpi-c`（图标图标💰🎯📉 + body[lab+main]，main内 span.v + deltaBadge 胶囊）；style.css 删除 .ov-kpi-b 系列改为 .ov-kpi-c 系列（含 k-spend/k-lead/k-cpl 图标底色、delta 胶囊底色）。smoke_web.js 的 kpiSpend 正则同步改为匹配新结构 `总消耗</div><div class="main"><span class="v">¥`。测试通过（筛选前后 KPI 变化、重置一致）。
- **筛选器改为3行×2列紧凑布局**（commit `5833928`）：用户截图显示右侧筛选器横向过宽导致左侧KPI卡空旷。修改：style.css 将 `.ov-top-kpis` 从 `repeat(4,1fr)` 改为 `1.15fr 1.15fr 1.15fr 0.9fr`（筛选卡收窄）；`.ov-filter-card .filter-bar` 改为 `display:grid; grid-template-columns:1fr 1fr; gap:8px`，select 和 button 宽度 100%。5个下拉+1个按钮自然排成3行2列：行1=行业/赛道，行2=品类/客户，行3=状态/应用筛选。冒烟测试通过。
- **当日监控筛选器整体移除**（commit `841545c`）：用户决定直接删掉筛选器（而非3×2紧凑排布）。app.js 删除 OV_FILTER 常量、ovApply/ovCustomers 函数、renderOverview 内 ovSel/ovKeep/ovOpts/ovInds…选项构建及筛选卡 HTML，CS 直接=CUSTOMERS；style.css `.ov-top-kpis` 回到 repeat(3,1fr)，删 `.ov-filter-card` 样式；smoke_web.js 删 ovApply 导出与筛选断言块。测试通过。注意：筛选功能已完全下线（客户列表页的 filter-bar 保留，仅当日监控页移除）。
- **撤销筛选器删除，仅去掉「筛选器」标题**（commits `c70a81e` revert + `2148b0a`）：用户澄清——"把筛选器去掉"指的是删掉卡片内的**「筛选器」三个字标题**，不是删整个筛选功能！先 `git revert 841545c` 恢复筛选器（回到3行×2列布局），再删除 `<div class="filt-title">筛选器</div>` 及对应 CSS。smoke 测试通过（筛选应用/重置断言恢复）。教训：用户说"去掉X"时若是标题字样类的小改，先确认对象范围，别急着删整个功能模块。
- **交接文档大更新：新增操作日志**（commits `23e4cad` + `8cb1190`）：用户要求确认每次修改都 git 了、并补充操作日志到交接文档。检查发现所有修改均已 git 提交（64条commit），但交接文档停在 v2 评测收尾状态，未覆盖 Phase 3 前端工作。更新内容：①新增第14节「操作日志」——全量64条commit按7个阶段分表记录（commit hash/时间/变更说明/影响文件）；②更新第1节进度——增加Phase 3前端6行里程碑；③更新第3节代码地图——新增api_server.py/web/tests/demo/data/6个目录说明；④更新第9节待办——Phase 3条目从"进行中"改为"已完成"；⑤新增第10节避坑7条（zsh cwd重置/后台进程/归因竞态/sim日期滞后/中文URL解码/ATTR_TOKEN/「去掉X」歧义）；⑥更新第12节命令——新增启动服务器/冒烟测试/sim刷新/种子重置4组命令；⑦第11节用户偏好新增铁律三原则；⑧第13节工作流新增操作日志维护规则。同时.gitignore新增web/reports/屏蔽，移除误入版本控制的运行时报告HTML。
- **审核闭环打通（RAG 飞轮入库口）**（commits `377a678` + `5322685`）：此前问答确认的唯一未完成核心 gap——「审核通过的报告不会自动沉淀为案例」。实现：①`agent/review_actions.py` 新增 `promote_to_case(conn, report_id)`：从 report_json 第5章 top3_detail 提取 anomaly_signature（search_cases 的 LIKE 匹配源）+ key_evidence_json，第7/8章提取 action_taken；anomaly 表空时兜底；幂等（重复调用返回 duplicated）。新增 `record_badcase()`：驳回理由入 diag_badcase（status=open），幂等。②`api_server.py` 新增 `POST /api/review`（approve→confirm+入库 / reject→必填理由+badcase）；POST /api/report 返回值加 report_id+case_count。③前端报告弹层底部审核条：「通过入案例库」/「驳回（必填理由）」+ 案例库条数显示。④smoke 测试新增审核断言；零成本评测 122/122 通过。验证：temp DB 上 approve report 24 后 /api/cases 立即能检索到新案例（signature="留资成本 + 在投笔记"）。RAG=案例库=diag_case 表（同一套东西），diag_badcase 物理隔离不参与检索。
- **周报呈现四项修改**（commits `9be87c9` + `6041652`，用户"先改好不要执行"）：①指标表后六项中文（button_rate→按钮率等，渲染层 METRIC_CN 映射，web/app.js + api_server.py）②分层诊断层级中文（LAYER_CN：placement→版位/plan→计划/note→笔记/funnel→漏斗）+ orchestrator `_layer_diagnosis` judgement 从"在投笔记双门槛"细化为带数字中文（"在投笔记数 12→8（-4篇/-33.3%），触发基建异动门槛；本期新建笔记 X 篇"；漏斗层"CTR +5.2%、按钮率 -3.1%…"；版位层"信息流 CPM +12.3%…"）③异常与原因删"置信度 高"显示（confidence 字段保留供内部权重，仅不展示，两处渲染同步删）④第 3 章加近 14 天目标成本折线图（app.js reportTrendChart + api_server._trend_svg 内联 SVG，同当日监控分面小图视觉；assemble_report 第 3 章新增 trend_metric 字段）。system_prompt.md 新增全中文命名红线+证据具体性红线。关键认知：八章节由代码组装（prompt 第 7 节明说），英文残留是渲染层直接显示数据字段名，非 LLM 输出——用户以为改 prompt 就行，实际需渲染层改。未跑任何 LLM 生成（遵守"不要执行"），仅静态检查+旧报告 JSON 干跑渲染验证。
- **周报呈现再优化（用户确认执行，commit `aa816ab` + 操作日志 `5fc1358`）**：①第3章双折线图（消耗趋势+目标成本趋势）；②异常与原因去 # 序号；③文本归一化（tools.norm_report_text/norm_top3：search→搜索版位、feed→信息流、贡献度裸小数→百分数如1.3036→130.36%、delta→带货币单位如"变化 ¥-245.92"），system_prompt 口径标签红线强化。根因修复：get_trend 仅支持派生指标导致消耗趋势全 None（derive_metrics 不返回原始 spend），新增原始量纲分支。端到端生成报告验证通过（双图+无#）。注意：用户上轮我误把旧报告 JSON 干跑当"新效果"，本次改为真正重新生成报告验证。
- **修复分享链接复制按钮白底白字 + 报告趋势图并排**（commit `c6681fb`）：用户截图显示报告详情页分享链接条中「复制链接」按钮呈白色空白（.report-actions button 统一 color:#fff 覆盖到 .link button，而复制按钮背景 #fff，导致白底白字）。修复：给 `.report-actions .link button` 单独加 `color:var(--ink);font-weight:700`。另将报告弹层第3章的消耗趋势+目标成本趋势两张 SVG 由上下堆叠改为并排（新增 `.report-trend-pair` flex 容器，单图宽度 420→340、高度 170→150，响应式 <640px 时恢复纵向）。
- **发现卡片点击错位问题（已修复，commit `1a1c2ed`）**：用户反馈部分客户卡片点进去显示的不是该客户信息。根因在 `web/app.js`：`applyFilter()` 渲染筛选后列表时把 filtered-list 的索引 `i` 传给 `openDetail(i)`，而 `openDetail()` 用该索引直接取全局 `CUSTOMERS[i]`，一旦启用筛选两者位置就对不上。修复：卡片点击改传 `customer_id`，新增 `openDetailById(id)`（用 `findIndex(c=>c.id==id)` 定位真实索引）再调 `openDetail`；`cardHTML(c,i)`→`cardHTML(c)`，`renderGrid` 同步去掉索引。无后端/LLM 成本。用户同轮问能否让 Claude 直接进本地站检查——本地 127.0.0.1 外部模型不可达，需公网暴露（Flask 后端非静态站点，CloudStudio 静态部署不适用）或改用本地导出文件/独立子代理审查。
- **本地服务器再次不可达 + 重启（task `0fqoye`）**：用户截图 `ERR_CONNECTION_REFUSED`。排查确认 `127.0.0.1:8000` 无监听，后台任务 `jU6KQ0` 已被环境回收。重新 `run_in_background` 启动 `python3 api_server.py`，curl 验证 200。原因：`api_server.py` 是本地开发服务器，非常驻服务，后台任务可能被清理；长期稳定需部署到 Python 运行环境主机。已写入长期笔记 `MEMORY.md`：含手动重启命令、代理 bypass 建议。
- **Gemini 复核 悦颜美容SPA 报告 + 我的核验**：用户让 Gemini 审报告，Gemini 列 8 条。经查 DB（report 45/46/47，CTR=0.0564、button_rate=0.17、lead_cvr=0.0077，summary 含"feed版位"）与代码核验，结论：真实问题 2 类——① 摘要(ch2 summary)未过 `norm_report_text`（norm 只覆盖 layer_diagnosis 与 top3_detail，未覆盖 summary/suggestions），故"feed版位"未转"信息流版位"；② 比率类指标(CTR/button_rate/open_rate/lead_rate/lead_cvr)在报告指标表以原始小数呈现、缺 %，前端 `renderReportModal` 未 ×100，与 overview(KPI ×100) 不一致。Gemini 误报/夸大：① 周期定义（实际 08-07~08-20 vs 07-24~08-06 都是14天，正确）；③ "计算偏差/精度丢失"实为仅缺 % 非算错；④ "无法核验在投计划数5→5"——明细页「基建情况」已透出该数，可核验；⑤ 贡献77.33% 已带% 正常。可信度 2.5/5 偏苛。已给用户优化版 prompt（只输出关键错误、可贴回网站反馈框）。
- **报告英文残留 + 比率补% + 分享页趋势并排修复**（commit 本轮）：用户反馈 3 点——① 核心结论残留 open_cost/lead_cost 英文；② 比率类指标(CTR/button_rate/open_rate/lead_rate/lead_cvr)纯小数漏 %；③ 新标签页(分享页)两张趋势图未横向并排。根因与修复：① `tools.norm_report_text` 仅覆盖 feed/search，新增 `METRIC_TEXT_CN`(open_cost→开口成本, lead_cost→留资成本)，并在 api_server.render_report_html 与 web/app.js 的 summary/top3/异常/建议/案例/行动计划「所有展示文本」统一调 norm（根治全局英文残留，非只核心结论）；② 后端 `RATE_METRICS`+前端 `RATE_METRICS` 集合，指标表对这 5 个比率 ×100 补 %（成本/CPM/CPC 等保持原值）；③ 分享页 render_report_html 两张 `_trend_svg` 包裹 `<div class="trend-pair">`（内联 CSS flex 并排，<640px 转纵向；弹层此前已并排）。额外健壮性：`_trend_svg` 加 None 防护，`top3_detail` 兼容 str/list（render 层与前端均加类型判断）。零成本重渲染全部 9 个 web/reports/*.html（仅调 render_report_html，不跑 LLM），验证 0 个文件残留 open_cost/lead_cost/feed/search 英文、trend-pair 已并排、CTR=5.64% 等。服务器已重启加载新代码（task `xNQ2Ke`）。注意：web/reports/ 被 .gitignore 忽略，重渲染产物不入库，仅落磁盘供线上查看。
- **审核重复入库 bug 修复**（commit 本轮）：用户反馈「已通过报告再次点开仍让勾选、且重复勾选使案例库条数增长」。根因两层：① 每次「生成报告」`orchestrator.run` 写死 `version=1` 且每次 `_new_task` 新建 report+task 行（orchestrator.py:144/514），同一客户多次生成拿到**不同 report_id**；而 `promote_to_case` 原只按 `source_report_id` 去重，按客户则漏 → 重复入库。② 隐藏更深的坑：报告 JSON 的 location 是**原始英文**(feed/lead_cost/open_cost)，而历史已入库案例的 `anomaly_signature` 是**归一化中文**(信息流/留资成本/开口成本)——`promote_to_case` 拼签名用原始文本，和已存签名对不上，去重失效。修复：`promote_to_case` 改为按 `(customer_id + 归一化签名)` 去重（新增 `_SIG_MAP` 内联归一化，与显示层一致），并对现有 diag_case 按 (客户+签名) 去重清理（删 1 条 customer_id=44 重复，从 6→5）；前端：`/api/report` 返回 `customer_has_case`，生成报告后若客户已入案例库则审核条置灰显示「✓ 已入案例库，无需重复审核」，通过后同样锁死。验证：临时副本上 promote 同一客户「旧报告+重新生成的新报告」两次均返回 duplicated、案例数不增。教训：**报告 JSON 存原始英文、显示层才归一化**这条设计，导致任何基于 JSON 文本做匹配/去重的逻辑都必须自行再归一化，否则会静默漏匹配。
- **修复 promote_to_case str/list 崩溃 + 批量补案例库**：排查"待审核报告批量补案例库"时发现 2 个事实——① 53 份 draft 报告中，**28 份是空壳**（top3_detail="（待 LLM 归因）"或空，集中在码上AI学堂 23 份评测稿），25 份为真实内容；② `promote_to_case` 假设 top3_detail/行动计划元素是 dict，遇到 str 元素会 `AttributeError: 'str' object has no attribute 'get'` 直接崩溃。修复：`agent/review_actions.py` 提取 top3/plan/sugg 时加 `isinstance(..., str)` 兜底（str→空、非 dict 元素过滤），空壳报告安全走"正常周"分支（但批量脚本层已排除空壳不入库）。执行（用户确认"全部真实报告"范围）：先备份真库到 /tmp/ad_review_backup_20260820_133946.db，再对 25 份真实 draft 报告批量 promote_to_case——**新增 15 条、跳过 10 条（同客户同签名已存在），案例库 5→20 条，去重客户 11 个**。新增 15 条 = 码上AI学堂 9 条（评测稿，用户已知）+ 智绘AI课堂/拾光婚纱/欢乐水世界/悦颜美容/燃动运动馆/洁到家家政 各 1 条。28 份空壳未入库。教训：draft 报告质量参差，批量入库前必须按"top3_detail 是否为有效 dict 列表"过滤，且入库函数要对 str/list 混合结构健壮。
- **重启服务器加载新代码**（无代码改动）：旧进程 PID 65823 占用 8000 端口（http_proxy 透传导致直连旧服返回 200、kill 后代理返 502）。`nohup python3 &` 方式在沙箱里会随 Bash 任务结束被回收，改用 Bash 工具原生 run_in_background 直接常驻。新进程 PID 69851（后台任务 1FzXF7）已监听 8000，直连(--noproxy)与代理探测均 HTTP 200，新代码（含 promote_to_case 健壮性修复 0c99d42）生效。教训：**本环境重启后端必须用 run_in_background 原生后台，不要用 nohup+&**。
- **部署方案调研（用户选"Cloud Studio 工作空间"）**：诊断台后端 `api_server.py` 整套（前端+API+分享页）仅一个进程，且**零第三方依赖**——`agent/llm.py` 用 `urllib` 直连 DeepSeek（环境变量 `DEEPSEEK_API_KEY`），DB 用本地 SQLite（`data/ad_review.db`，含20条RAG案例）。`api_server.py` 默认 `--host 127.0.0.1`（仅本机），远程需 `--host 0.0.0.0`。关键限制：**本环境 CloudStudio 一键部署工具只支持纯静态站点，跑不了 Python 后端**，无法从聊天里一键开通后端常驻。已交付 `start.sh`（崩溃自重启循环 + 0.0.0.0 + 环境变量校验）和 `部署说明.md`：用户需在 Cloud Studio 工作空间（持续运行环境）或任意有 Python3 的服务器上传整个 诊断台/ 目录（含 data/，被 gitignore 忽略部署时务必一起拷）、`export DEEPSEEK_API_KEY` 后 `./start.sh` 即可常驻。教训：本地沙箱后台任务会被回收，真·常驻必须落到持续运行的宿主。
- **静态只读快照上线（解决"别人随时看"，commit 已提交）**：用户只有 Mac、无额外常开机器，CloudStudio 静态部署（之前购买的能力，历史 share 链接验证仍 200 持久）跑不了 Python 后端。方案：把大盘导出成静态快照部署。新增 `static_export.py`（零成本：复用 api_daily/api_customers/api_attrib/api_base + 各客户最新 report_json，不调 LLM）生成 `web/snapshot.json`（50客户+日度序列+50归因+16报告）。改 `web/app.js`：boot 检测不到 `/api/*` 时自动回退读 snapshot.json（离线只读模式），归因/基建/报告兼容快照，新增 `?offline=1` 调试开关，隐藏手动录入、生成按钮变"查看报告（静态快照）"。用 `workbuddy_cloudstudio_deploy` 部署 `诊断台/web/` 成功（shareLink https://349a08f059e14b86937b3346fa9a6d15.app.workbuddy.link，verified）。局限：只读，生成/审核仍需本地后端；数据更新需重跑 static_export 并重部署。教训：本环境本地后台任务会被回收，真·常驻必须用用户已买的 CloudStudio 静态托管（持久）做只读快照，或另备 Python 主机跑实时后端。

- 静态快照线上不齐全根因修复（commit daf19ae）：用户反映线上静态站打开内容不全。排查发现 static_export.py 第36行 daily=daily_resp["customers"] 已取内层 {客户名: 日度序列} 存入 snapshot.daily，但前端 loadSnapshot(app.js:802) 误写 DAILY=daily.customers||{}，导致离线模式 DAILY 为空，全部50客户的卡片/KPI/趋势/日表全空白。在线后端路径(769行 DAILY=daily.customers)本就正确。修复：离线路径改为 DAILY=daily，已重新 cloudstudio 部署生效。另：快照 cpm 字段缺失但前端从不读 r.cpm 无影响；50客户仅16份报告（其余点查看报告显示暂未生成，属预期非bug）。

- RAG 召回 EN/CN 签名静默漏匹配 bug 修复（commit 386fa3c）：入库签名在 review_actions._norm_sig 把 open_cost→开口成本/lead_cost→留资成本 归一化存中文；但召回端 orchestrator.py:261 传原始英文 e["metric"]，search_cases 直接用英文 LIKE，导致 open_cost/lead_cost 类永远匹配不上。修复：tools.py 新增 SIG_MAP_EN2CN/norm_sig_term 作唯一来源，search_cases 查询前归一化；review_actions 改复用 tools 映射消除漂移。验证 open_cost/lead_cost 现可命中含中文签名的案例；零成本评测 126/126 通过。

- 交接文档同步（用户指令：写操作日志 + 剩余部署问题写待办）：操作日志补齐 8/20 10:47 之后的 15 个缺失提交(27614b4→386fa3c) + 本轮问答条目；目录头计数 97→101、截至 0c99d42→386fa3c；待办节第 9 节新增「部署与对外访问待办」子节（是否对外部署待决策 / 本地会被沙箱回收坑 / 实时后端部署步骤 / 真实实习报告入库 RAG）。本地服务器重启(task lNCN2C, PID 99706)供用户检查手动录入；说明：沙箱后台进程空闲仍会被回收，真·常驻须用静态快照或公网主机。
