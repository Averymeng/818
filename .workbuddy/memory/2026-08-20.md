- **问题诊断：今日消耗全 0 + 归因计算中**（无代码改动）：用户截图显示 8-20 客户每日监控全部 ¥0/-100%，且增量 TOP 卡“归因计算中…”。根因是真实时间已到 8-20，但 SQLite 模拟数据最新只到 8-19（refresh_sim 未跑今天）。因此今日窗口无数据，所有客户环比 −100%；前端 loadAttrib 请求后端 /api/attrib 实际可返回文案，但因页面可能未重载或当天全 0 显得无变化。解决：执行 `python3 data/refresh_sim.py --run-once` 重新生成到 8-20，DB 最新日期变为 2026-08-20。验证 /api/attrib 返回真实归因（如 cid1 “消耗环比 -4%：留资成本 ¥53→¥42”）。建议用户刷新浏览器。
- **销售业绩模块删除 + 趋势模块改造demo**（commit `0b5d713`）：用户确认删除“销售业绩”卡片。app.js 去掉 MONTH_TARGET/sales/pctDone/remain 及整段卡片渲染（保留顶部 KPI 的 `sales=cur.spend`），style.css 将 `.ov-row2` 从 2fr 1fr 改为 1fr（趋势卡独占整行），删除 `.ov-progress` 系列样式。另新增 `demo/趋势模块改造demo_分面小图.html`：用 8-07~8-20 真实 DB 快照（每日 spend/lead/open 汇总）做 3 张独立小图，各自带真实 Y 轴、均值虚线、末端点标注，替代现在“3 条不同量纲线各自归一化叠加”的不可读画法。
- **趋势模块平滑曲线上线**（commit `0de8e79`）：把首页趋势卡改成 3 张独立小图（消耗/留资成本/开口成本），各自带真实 Y 轴刻度、均值虚线、末端点标注；线条统一用 Catmull-Rom 转贝塞尔的 `smoothPath()` 画成平滑曲线，并加了极淡的面积填充。样式新增 `.ov-trend-grid/.ov-trend-mini/.ov-t-axis/.ov-trend-empty`。smoke 测试通过。
